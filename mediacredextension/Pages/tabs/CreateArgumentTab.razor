@using MediaCred;
@inject IApiData _apiData
<MudStack>
    <MudTextField @bind-Value="title" T="string" Label="Title"></MudTextField>
    <MudTextField @bind-Value="publisher" T="string" Label="Publisher"></MudTextField>
    <MudTextField @bind-Value="claim" T="string" Label="Claim"></MudTextField>
    <MudTextField @bind-Value="ground" T="string" Label="Ground"></MudTextField>
    <MudTextField @bind-Value="warrant" T="string" Label="Warrant"></MudTextField>
    <MudIconButton OnClick="async ()=> await CreateArgument()" Icon="@Icons.Filled.Add"></MudIconButton>
</MudStack>



@code{
    private string title;
    private string publisher;
    private string claim;
    private string ground;
    private string warrant;

    private async Task CreateArgument()
    {
        //var article = new Article() { Title = title, Publisher = publisher };
        //await _apiData.CreateArgument(article, argument);


    }
}